version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "REPLACED_AT_DEPLOY"
        LoadBalancerInfo:
          ContainerName: "image-app"
          ContainerPort: 8082